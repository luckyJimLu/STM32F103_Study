# ==============================================================================
# third_party/Make.defs - Third-Party Repositories Build Definitions
# ==============================================================================

ifeq ($(CONFIG_USING_CJSON),y)
include $(TOPDIR)/third_party/cJSON/Make.defs
endif

ifeq ($(CONFIG_USING_LWIP),y)
include $(TOPDIR)/third_party/lwip/Make.defs
endif

ifeq ($(CONFIG_USING_SEGGER_RTT),y)
include $(TOPDIR)/third_party/segger_rtt/Make.defs
endif

ifeq ($(CONFIG_USING_EASYLOGGER),y)
include $(TOPDIR)/third_party/easylogger/Make.defs
endif

ifeq ($(CONFIG_USING_LETTER_SHELL),y)
include $(TOPDIR)/third_party/letter_shell/Make.defs
endif
