# Deprecated location

Third-party sources belong in the repository-level `third_party/` directory.
This directory no longer participates in Kconfig or the build graph and remains
only as a migration marker.
