# ==============================================================================
# middlewares/Make.defs - Middlewares Layer Build Definitions
# ==============================================================================

ifeq ($(CONFIG_RTOS_RTTHREAD),y)
include $(TOPDIR)/middlewares/rtos/rt-thread/Make.defs
endif

ifeq ($(CONFIG_RTOS_FREERTOS),y)
include $(TOPDIR)/middlewares/rtos/freertos/Make.defs
endif

include $(TOPDIR)/middlewares/third_party/Make.defs
