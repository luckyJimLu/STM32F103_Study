from pathlib import Path


ROOT = Path(r"D:\Project\STM32F103_Study")
OUT = ROOT / "docs" / "board_resources" / "sops"
OUT.mkdir(parents=True, exist_ok=True)

SOURCE = "《STM32F1开发指南（精英版）- HAL库版本 V1.2》"


# ch, slug, title, pages, resources, prerequisites, rtos_design, specific_steps,
# acceptance, risks, level
EXPERIMENTS = [
    (6, "led-marquee", "跑马灯", "140-155", "DS0/PB5、DS1/PE5，GPIO", "工程模板、时钟、GPIO、毫秒时基", "LED 服务任务或低频周期任务；用相对命令队列控制灯态，用 RTOS 延时替代阻塞循环。", ["配置 PB5、PE5 为推挽输出并确认 LED 有效电平。", "先完成单灯亮灭，再实现双灯交替。", "迁移到 RTOS 后用固定周期唤醒，避免在任务中空转延时。"], "两灯按设定节拍稳定交替；运行 10 分钟无节拍漂移或任务饥饿。", "板载 LED 可能为低电平点亮；不要让高优先级任务执行忙等待。", "直接适合"),
    (7, "buzzer", "蜂鸣器", "156-160", "有源蜂鸣器/PB8，GPIO", "GPIO、命令输入", "建立蜂鸣器执行器服务；队列接收开/关/持续时间，软件定时器负责自动关闭。", ["配置 PB8 为推挽输出并确认静音电平。", "验证持续鸣叫、停止和定时鸣叫。", "把上层提示音请求封装为非阻塞命令。"], "各类命令均正确执行，定时鸣叫结束后自动关闭。", "复位阶段避免误鸣；多个调用者不得直接争用 PB8。", "直接适合"),
    (8, "key-input", "按键输入", "161-166", "KEY0/PE4、KEY1/PE3（低有效），KEY_UP/PA0（高有效）", "GPIO 输入、上下拉、系统时基", "10-20 ms 周期按键扫描任务完成去抖，向应用发送按下、释放、长按事件。", ["按有效电平配置 PE4、PE3、PA0 的输入及上下拉。", "实现稳定状态、候选状态和去抖计数。", "将边沿和长按转换为统一键值事件。"], "每个键单击 50 次无明显误触发；长按只产生约定事件。", "不要用长延时去抖；PA0 还用于唤醒和 TIM5_CH1，后续实验要统一分配。", "直接适合"),
    (9, "uart", "串口通信", "167-183", "USART1/PA9(TX)、PA10(RX)，P3 跳帽，DS0", "GPIO 复用、USART、PC 串口工具", "串口服务任务独占发送；RX 中断或 DMA 写环形缓冲区，再通知解析任务。", ["确认 P3 的 RXD/TXD 跳帽连接到 PA9/PA10，并设置一致的串口参数。", "先验证轮询收发，再改为中断或 DMA 接收。", "实现环形缓冲、行协议或帧协议，并为发送端加互斥或单写者队列。"], "持续收发 10 KB 数据无丢字节、乱码和死锁；错误帧可恢复。", "ISR 只搬运/通知；printf 可能不可重入且耗栈，量产代码应使用受控日志接口。", "直接适合"),
    (10, "exti", "外部中断", "184-190", "KEY0/PE4、KEY1/PE3、KEY_UP/PA0，EXTI", "GPIO、NVIC、按键电平", "EXTI ISR 仅清标志并发送任务通知；输入任务完成去抖和业务处理。", ["配置对应 EXTI 线、触发边沿和 NVIC 优先级。", "在回调中记录事件并立即退出。", "由按键任务二次采样确认稳定电平，再发布键值事件。"], "三键可可靠唤醒任务；抖动不会造成重复业务动作。", "同一 EXTI 线的端口映射互斥；调用 RTOS ISR API 时必须满足内核中断优先级规则。", "直接适合"),
    (11, "iwdg", "独立看门狗 IWDG", "191-196", "IWDG、DS0、KEY_UP", "LSI、复位原因读取、关键任务心跳", "看门狗监督任务收集关键任务心跳，仅在全部健康时喂狗。", ["按 LSI 和目标超时时间计算预分频与重装值。", "记录并显示复位来源，验证停止喂狗会复位。", "为每个关键任务分配心跳位，由监督任务在窗口内汇总。"], "正常运行不误复位；故意挂起任一关键任务后，在设计超时内复位并保留原因。", "不要让任意任务直接喂狗，否则系统局部失效会被掩盖；调试暂停会触发复位。", "适合但需改造"),
    (12, "wwdg", "窗口看门狗 WWDG", "197-203", "WWDG、DS0、DS1", "PCLK1、WWDG 时序、复位原因", "高可靠监督任务或定时回调在允许窗口内刷新；健康条件仍由任务心跳汇总。", ["根据 PCLK1 计算计数频率、窗口值和复位边界。", "验证过早刷新和过晚刷新均会导致预期结果。", "将刷新时刻与调度抖动预算结合，并保留足够裕量。"], "正常负载和压力负载下均在窗口内刷新；注入调度阻塞后能够复位。", "窗口过窄会因调度抖动误复位；软件定时器回调被阻塞时也可能错过窗口。", "适合但需严谨设计"),
    (13, "timer-interrupt", "定时器中断", "204-212", "TIM3、DS0、DS1", "定时器时钟树、NVIC", "硬件定时器 ISR 递增计数或释放通知；任务完成较重处理。", ["核对 APB1 分频及定时器时钟倍频规则。", "配置 TIM3 更新中断并测量实际周期。", "把 LED/日志等处理移出 ISR，只保留清标志与通知。"], "示波器或时间戳验证周期误差满足要求；高负载下无中断风暴。", "HAL 回调可能共享多个定时器；必须判断实例并及时清除标志。", "直接适合"),
    (14, "pwm", "PWM 输出", "213-221", "TIM3_CH2，经部分重映射到 PB5/DS0", "TIM3、GPIO 复用、重映射", "PWM 由硬件持续输出；控制任务通过队列更新频率和占空比。", ["完成 TIM3 部分重映射并将 PB5 配置为复用推挽。", "分别验证频率和占空比边界。", "提供线程安全的 PWM 设置接口，必要时在更新事件同步装载。"], "测得频率、占空比与设置值一致，0%/100% 边界行为明确。", "PB5 与 DS0 共用；频率改变时 PSC/ARR/CCR 的更新次序会产生毛刺。", "直接适合"),
    (15, "input-capture", "输入捕获", "222-233", "TIM5_CH1/PA0，DS0；可将 PB5 PWM 接到 PA0", "TIM5、NVIC、测试信号", "捕获 ISR 将时间戳/溢出信息送入队列，计算任务求周期、频率和脉宽。", ["配置 PA0 为 TIM5_CH1 输入捕获并处理计数器溢出。", "可用导线将 PB5 的 PWM 输出接至 PA0 做闭环验证。", "为捕获队列设计溢出策略，计算任务过滤异常间隔。"], "在目标频率范围内测量误差满足要求；断线、停波后能超时报告。", "PA0 同时是 KEY_UP 和待机唤醒；高频信号可能使中断/队列过载。", "直接适合"),
    (16, "capacitive-touch", "电容触摸按键", "234-242", "TPAD、TIM5_CH2/PA1，P7 跳帽", "定时器计时、基线校准、GPIO", "专用触摸采样任务做充放电计时、基线追踪和去抖，向输入事件队列发布状态。", ["确认 P7 将 TPAD 信号接到目标引脚，并执行上电基线采样。", "测量未触摸与触摸时的计数分布，确定带滞回阈值。", "以固定周期采样并处理环境漂移，避免在临界区长时间等待。"], "不同手指和连续运行条件下触摸可靠，释放后可恢复；无明显漂移误触。", "PA1 也用于 ADC 实验；温湿度变化影响阈值，需动态基线和超时保护。", "适合但需时序隔离"),
    (17, "oled", "OLED 显示", "243-259", "OLED：CS/PD6、RST/PG15、RS/PD3、WR/PG14、RD/PG13，P4 接口", "GPIO 总线、字库/绘图接口", "显示任务独占 OLED；其他任务提交绘图命令或更新模型，统一刷新。", ["核对模块方向和 P4 接口，完成复位与初始化序列。", "验证点、线、字符和清屏原语。", "建立显示命令队列；高频状态只保留最新值，避免队列堆积。"], "字符/图形显示正确；多任务同时请求显示时无撕裂、死锁或总线冲突。", "GPIO 模拟总线占用 CPU；不要从 ISR 或多个任务直接调用底层写函数。", "直接适合"),
    (18, "tft-lcd", "TFTLCD 显示", "260-287", "板载 LCD 接口、FSMC、背光", "FSMC、LCD 控制器识别、绘图层", "UI/显示任务独占控制器；通过消息更新界面，采用脏矩形或条带缓冲降低 SRAM 压力。", ["插接 LCD 后读取控制器 ID，选择匹配初始化表。", "验证方向、颜色、点线矩形和字符显示。", "将所有绘图汇聚到显示任务，测量一次刷新最坏耗时。"], "全屏色块和文本方向正确；压力刷新时输入、通信任务仍能按期运行。", "不同 LCD 控制器初始化不同；STM32F103 SRAM 有限，不宜默认建立全屏帧缓冲。", "适合但需资源规划"),
    (19, "usmart", "USMART 调试组件", "288-299", "USART1、TFTLCD、DS0/DS1", "串口服务、命令注册、显示", "CLI 解析任务从串口流读取命令，通过注册表和消息调用业务服务。", ["先验证串口行输入和回显，再注册无副作用的测试命令。", "给参数解析加入数量、范围和错误提示校验。", "耗时命令改为异步请求，CLI 只返回受理状态或结果通知。"], "合法命令执行正确；畸形、超长命令不会越界或阻塞系统。", "调试命令可改变硬件状态，应区分开发/发布构建并限制危险操作。", "适合但需服务化"),
    (20, "rtc", "RTC 实时时钟", "300-316", "RTC、后备域、备份电池、TFTLCD", "LSE/LSI、BKP、时间换算", "时间服务任务提供线程安全读写；RTC 秒/闹钟中断只通知订阅任务。", ["安装后备电池，配置时钟源并只在首次启动时初始化时间。", "验证断电保持、闰年和日期进位。", "封装时间读写、闹钟和订阅接口，禁止各任务直接改后备域。"], "主电源断开再恢复后时间保持；闹钟能可靠唤醒处理任务。", "重复初始化会覆盖保留时间；LSE 起振失败需超时和回退策略。", "直接适合"),
    (21, "standby-wakeup", "待机唤醒", "317-324", "PWR、KEY_UP/PA0、DS0、TFTLCD", "RTC/唤醒脚、状态保存、复位原因", "电源管理器协调任务停机、外设关断和状态保存；唤醒后按冷启动加恢复路径处理。", ["确认 PA0 唤醒极性并清除唤醒/待机标志。", "进入待机前广播 suspend，等待存储和通信安全停机。", "唤醒后读取原因，恢复时钟、外设和必要应用状态。"], "系统可重复进入/退出待机；数据不损坏，唤醒原因记录正确。", "待机唤醒通常等效复位；若未先卸载文件系统或停止写入，可能破坏数据。", "适合但需系统级改造"),
    (22, "adc", "ADC", "325-337", "ADC1_CH1/PA1，P7 ADC 跳帽，TFTLCD", "ADC 校准、参考电压、采样时间", "定时器触发 ADC，DMA 填充缓冲；采集任务等待半满/满通知并发布滤波结果。", ["确认 P7 接线，输入电压严格限制在 0-3.3 V。", "执行 ADC 校准，验证单次采样与电压换算。", "再切换到定时器触发和 DMA 批量采样，明确缓冲区所有权。"], "已知电压点测量误差符合预期；连续采样无丢块且 CPU 占用可接受。", "严禁 5 V 输入；源阻抗过高需要更长采样时间，PA1 与 TPAD 共用。", "直接适合"),
    (23, "internal-temperature", "内部温度传感器", "338-341", "ADC 内部温度通道、TFTLCD", "ADC、内部参考/校准、温度公式", "低频传感任务通过 ADC 服务采样，多次平均后发布芯片温度。", ["启用内部温度通道并设置足够采样时间。", "丢弃启用后的首批样本，做平均和公式换算。", "以秒级周期采样，向监控服务提供趋势而非环境温度。"], "温度随芯片负载/加热产生合理趋势，数值无大幅跳变。", "这是芯片结温估计，不是精密环境温度；不同芯片需校准。", "直接适合"),
    (24, "light-sensor", "光敏传感器", "342-347", "光敏输入 PF8/ADC3_IN6，TFTLCD", "ADC3、GPIO/模拟配置、滤波", "周期传感任务调用 ADC 服务并做低通/滞回，发布光照等级事件。", ["确认 PF8 配置为模拟输入。", "记录亮、暗环境的 ADC 范围；本板通常越亮电压越低。", "加入平均、滞回和等级映射，避免阈值附近频繁切换。"], "覆盖传感器时读数向暗端变化，照明时向亮端变化；等级输出稳定。", "只能表示相对亮度；器件离散、供电和环境光会改变阈值。", "直接适合"),
    (25, "dac", "DAC", "348-358", "DAC_OUT1/PA4，TFTLCD/示波器", "DAC、定时器、可选 DMA", "静态输出由 DAC 服务设置；波形输出使用定时器触发 DMA，任务只更新缓冲。", ["配置 PA4 为模拟输出并先验证几个静态码值。", "用万用表/示波器核对电压换算和输出范围。", "若输出波形，建立双缓冲并在 DMA 通知后安全换缓冲。"], "静态电压误差合理；波形连续且频率、幅值符合设置。", "PA4 与 MPU6050 中断脚可能共用；DAC 驱动能力有限，外接负载需缓冲。", "直接适合"),
    (26, "dma", "DMA", "359-370", "DMA、USART/内存缓冲、DS0", "DMA 通道映射、中断、缓存所有权", "DMA 完成/半完成 ISR 仅通知；工作任务处理缓冲，采用 ping-pong 或环形缓冲。", ["根据外设核对固定 DMA 通道映射和传输方向。", "先验证一次传输，再启用循环或双缓冲模式。", "定义生产者/消费者所有权和溢出统计，禁止传输期间改写缓冲。"], "长时间连续传输内容一致；无越界、覆写、漏通知。", "F1 DMA 通道固定且会冲突；栈上缓冲在异步传输结束前可能失效。", "直接适合"),
    (27, "i2c-eeprom", "IIC", "371-382", "AT24C02，软件 IIC PB6/PB7", "GPIO 开漏、ACK、EEPROM 写周期", "I2C 总线服务或互斥保护事务；事务结束前不得释放缓冲。优先评估硬件 I2C。", ["确认 PB6/PB7 上拉和开漏释放逻辑。", "实现起止、ACK、读写字节，并验证 EEPROM 页边界。", "加入总线恢复、超时和写周期轮询，再封装线程安全事务接口。"], "跨页写入后回读一致；拔除器件或总线被拉低时能超时恢复。", "软件 I2C 的短时序不能被任意抢占；不要用长全局关中断，EEPROM 有写寿命。", "适合但需总线仲裁"),
    (28, "spi-flash", "SPI", "383-394", "SPI2/PB13、PB14、PB15，板载 SPI NOR Flash", "SPI、片选、Flash 擦写规则", "SPI 总线管理器用互斥量保护完整事务；Flash 工作任务串行执行读写擦除。", ["读取 JEDEC ID 并核对器件容量。", "验证页写、扇区擦除、跨页读写和忙状态轮询。", "将片选、总线配置和缓冲生命周期纳入单个事务。"], "随机地址写入/回读一致；超时和写保护错误能报告。", "SPI2 还可能被无线模块共享；擦除耗时长，持锁策略要避免饿死实时业务。", "适合但需总线仲裁"),
    (29, "rs485", "RS485 通信", "395-403", "USART2/PA2、PA3，收发方向 PD7，RS485 接口", "UART、半双工方向、总线终端", "RS485 服务任务维护发送-等待完成-切回接收状态机，队列化所有报文。", ["确认 A/B 极性、共地和总线终端，设置一致串口参数。", "发送前置发送方向，等待 TC 而非仅 TXE 后再切回接收。", "加入帧超时、CRC、重试和总线静默时间。"], "双节点双向通信稳定；连续收发不截断尾字节，不出现双方同时驱动。", "方向切换过早会丢尾字节；多节点协议必须定义地址、仲裁和超时。", "适合但需状态机"),
    (30, "can", "CAN 通信", "404-433", "CAN/PA11、PA12，收发器、终端电阻", "CAN 时钟、位时序、过滤器、物理总线", "CAN RX ISR 将帧推入队列；协议任务解析，TX 由队列串行提交并处理邮箱完成。", ["确认收发器供电、CANH/CANL 和总线两端 120 Ω 终端。", "计算位时序并验证过滤器、回环和双节点通信。", "设计 RX 队列容量、总线关闭恢复、错误计数和优先级策略。"], "目标波特率下长期无异常错误增长；总线断开、恢复时系统可自愈。", "PA11/PA12 与 USB 共用，不能同时启用；错误风暴时必须限流日志。", "直接适合"),
    (31, "touchscreen", "触摸屏", "434-458", "电阻/电容触摸控制器、TFTLCD", "LCD、触摸底层、校准参数", "触摸采样任务产生统一坐标/按压事件，UI 任务消费；校准参数由存储服务持久化。", ["完成控制器识别和原始坐标读取。", "执行多点校准，验证旋转方向和坐标映射。", "加入去抖、移动阈值和事件队列，显示任务不得阻塞采样。"], "全屏各区域点击准确；拖动连续，旋转后映射正确；重启后校准保留。", "触摸类型/控制器可能随屏幕变化；SPI/FSMC 访问与显示刷新要协调。", "直接适合"),
    (32, "infrared-remote", "红外遥控", "459-468", "红外接收 PB9/TIM4_CH4", "输入捕获、遥控协议、超时", "捕获 ISR 记录脉冲宽度到队列，解码任务完成协议状态机并发布按键事件。", ["用示波器或时间戳确认接收头输出极性。", "捕获引导码、数据位和重复码的脉宽。", "解码任务设置帧超时、容差和未知码统计。"], "常用按键、长按重复码识别稳定；噪声脉冲不会卡死状态机。", "逐边沿打印日志会破坏实时性；队列满时应丢弃整帧并重新同步。", "直接适合"),
    (33, "ds18b20", "DS18B20 温度传感器", "469-477", "单总线 PG11、DS18B20", "微秒计时、1-Wire、CRC", "传感任务采用启动转换-延时等待-读取的分阶段状态机；微秒槽由定时器或短临界段保证。", ["验证复位脉冲和存在脉冲。", "启动温度转换后释放 CPU，按分辨率等待完成。", "读取 scratchpad、校验 CRC 并转换温度，失败时重试/重枚举。"], "室温读数稳定；断开传感器能报告离线；CRC 错误不作为有效数据。", "1-Wire 微秒时序易受抢占影响；禁止在整个温度转换期间忙等待或关中断。", "适合但需时序改造"),
    (34, "dht11", "DHT11 温湿度传感器", "478-485", "单线 PG11、DHT11", "微秒计时、GPIO 双向、校验", "高优先级短采集例程或定时器捕获收集脉宽，传感任务低频触发并发布结果。", ["发送启动信号并验证传感器响应。", "测量 40 位脉冲宽度，按阈值还原数据并校验和。", "限制采样频率，给超时、离线和重试设置明确状态。"], "连续采样数据合理且校验通过；传感器拔除后不会永久阻塞。", "DHT11 采样慢且时序敏感；与 DS18B20 共用 PG11 时只能二选一或外部切换。", "适合但需时序改造"),
    (35, "mpu6050", "MPU6050 六轴传感器", "486-507", "I2C PB10/PB11，INT/PA4，AD0/PA15，ATK-MPU6050", "I2C、姿态算法/DMP、定时采样", "50 Hz 左右传感任务或数据就绪通知；I2C 互斥，原始/姿态数据经队列发布，DMP 任务预留较大栈。", ["确认 4.7 kΩ 上拉；AD0 为低时地址通常是 0x68。", "读取 WHO_AM_I，完成量程、滤波和采样率配置。", "先发布原始六轴数据，再启用 DMP/FIFO，并监测 FIFO 溢出。"], "静止时加速度和姿态稳定；移动响应及时；运行 30 分钟无 FIFO 溢出和栈溢出。", "PA15 默认是 JTAG/JTDI，需保留 SWD 的前提下释放；PA4 与 DAC 冲突，DMP 占栈较多。", "适合但需资源规划"),
    (36, "wireless", "无线通信", "508-520", "NRF24L01：SPI2，CE/PG8、CSN/PG7、IRQ/PG6", "SPI、IRQ、射频频道/地址", "无线服务任务独占收发状态机；IRQ 仅释放通知，SPI2 通过总线互斥与 Flash 共享。", ["读取配置寄存器验证模块和 SPI 通信。", "两节点设置相同频道、地址和数据率，先验证固定载荷。", "加入 IRQ 处理、重发/丢包统计、接收队列和应用层序号。"], "双向传输稳定，丢包率可测；模块断开或对端离线不会阻塞其他任务。", "供电去耦和布局会显著影响可靠性；无线与 Flash 共用 SPI2 时片选和事务必须严格隔离。", "适合但需总线仲裁"),
    (37, "flash-eeprom", "FLASH 模拟 EEPROM", "521-533", "STM32 内部 Flash", "Flash 页结构、擦写、链接布局", "参数存储服务串行化读写，使用双页/日志、版本号和 CRC；不得在 ISR 写 Flash。", ["从链接脚本划出专用页并核对不会覆盖程序。", "设计记录头、序号、CRC 和有效标志，先写后切换。", "测试掉电点、页满迁移、磨损统计和恢复路径。"], "反复更新和复位后读取最新完整记录；注入中断写入后仍可恢复旧值或新值。", "擦写会阻塞取指且寿命有限；地址布局必须与具体容量和 Bootloader 分区一致。", "适合但需事务化"),
    (38, "camera", "摄像头", "534-552", "OV7725 摄像头、P6、TFTLCD、FIFO/控制信号", "摄像头寄存器、像素时钟、显示、外部 FIFO", "摄像头任务控制帧采集，完成中断通知；帧/行缓冲通过有界队列送显示或处理任务。", ["核对 P6 方向和所有控制/数据线，读取摄像头 ID。", "配置低分辨率/低帧率，先验证彩条或实时预览。", "测量采集、搬运、显示耗时，设置丢帧和背压策略。"], "预览颜色和方向正确；持续运行不花屏、死锁或耗尽内存，丢帧可统计。", "F103 SRAM 和总线带宽很紧；避免整帧多份拷贝，优先 FIFO+条带/行缓冲。", "可行但资源紧张"),
    (39, "memory-management", "内存管理", "553-563", "SRAM、内存池、TFTLCD/串口", "链接内存布局、堆栈监测", "优先固定块内存池；统一封装分配器和互斥，ISR 禁止动态分配，并监控各任务栈水位。", ["统计静态区、主栈、RTOS 堆和各任务栈的边界。", "验证分配、释放、合并和越界检测。", "做碎片压力测试，记录剩余堆和任务高水位。"], "长时间随机分配释放无泄漏/损坏；栈与堆水位可观测并留有裕量。", "多个堆实现混用容易误释放；动态分配带来碎片和不确定时延。", "适合但建议静态化"),
    (40, "sd-card", "SD 卡", "564-582", "板载 SD 卡座/SDIO，KEY0、串口、TFTLCD", "SDIO、DMA、卡检测、块读写", "块设备服务串行处理请求；DMA 完成中断给信号量，插拔事件触发状态机。", ["使用全尺寸 SD 卡或可靠的 TF 转接卡，验证卡识别和容量。", "完成单块、多块读写及数据校验。", "加入超时、重试、插拔和 DMA 缓冲生命周期管理。"], "多块随机读写回读一致；拔卡不会死锁，重插可重新初始化。", "热拔写入会损坏数据；不同卡时序差异大，DMA 通道和内存对齐需核对。", "适合但需块服务"),
    (41, "fatfs", "FATFS 文件系统", "583-598", "FATFS R0.11、SD 卡、SPI Flash、TFTLCD/串口", "块设备、挂载、时间戳、编码", "文件系统服务任务或全局递归互斥保证单一所有权；应用通过文件请求队列访问。", ["先验证块设备，再格式化/挂载测试介质。", "完成创建、写入、同步、关闭、读取、目录遍历和错误路径。", "为断电场景规定 fsync/close 策略，并在待机/USB 切换前卸载。"], "重启后文件可读且内容一致；容量不足、拔卡和非法路径能明确报错。", "FATFS 是否可重入取决于配置；同一介质不能同时由 USB 主机和 MCU 文件系统写入。", "适合但需单一所有权"),
    (42, "chinese-display", "汉字显示", "599-614", "TFTLCD、中文字库、SPI Flash/SD", "显示、存储、编码/字模", "UI 任务负责绘制；字模服务从存储读取并缓存热点字符，避免持显示锁等待慢存储。", ["明确输入编码、字库格式、字号和字模寻址公式。", "验证常用字、边界码和缺字占位符。", "加入小型字形缓存，并让存储读取与显示提交分阶段进行。"], "中英文混排位置正确；缺字不会越界；连续翻页无明显卡顿。", "GBK/Unicode 混用会查错字模；缓存和字库占用需计入有限 SRAM/Flash。", "直接适合"),
    (43, "picture-display", "图片显示", "615-628", "TFTLCD、SD/SPI Flash、图片文件", "FATFS、图片格式、颜色转换", "解码工作任务按行/条带读取，显示任务消费像素块；有界队列提供背压。", ["验证 BMP/JPEG 等实际支持格式、文件路径和像素格式。", "采用小块读取和行缓冲，完成裁剪、缩放或居中策略。", "测量存储、解码、显示各阶段耗时并避免整图复制。"], "目标图片正确显示；损坏/过大文件可拒绝或中止，系统内存保持稳定。", "高分辨率 JPEG 解码可能超出 SRAM/栈；大文件不可一次性读入。", "可行但资源紧张"),
    (44, "camera-photo", "照相机", "629-640", "摄像头、TFTLCD、SD 卡、按键", "摄像头、FATFS、图片编码/保存", "采集、预览、编码/存储分为流水任务；有界队列和缓冲池控制背压。", ["先分别验证预览和文件写入，再联调拍照流程。", "按键事件触发一次采集，生成唯一文件名并写临时文件。", "写入成功后同步并原子确认；失败时释放所有缓冲和句柄。"], "每次按键只生成一张可打开图片；存储慢/满/拔卡时有明确提示且预览可恢复。", "同时预览和保存容易耗尽带宽/内存；写文件期间必须处理掉电和介质移除。", "可行但资源紧张"),
    (45, "handwriting", "手写识别", "641-652", "触摸屏、TFTLCD、识别库/字库", "触摸事件、显示、内存管理", "输入任务采集轨迹，UI 任务绘制，识别工作任务处理已封口笔画并返回候选。", ["校准触摸并采集按下、移动、抬起的有序点列。", "对轨迹做限点、归一化和笔画分割。", "通过消息触发识别，结果返回 UI；设置超时和取消。"], "常用样例能返回候选；快速连写不会丢事件或阻塞 UI，内存无泄漏。", "识别库可能耗栈、耗时且有授权限制；轨迹队列必须限长。", "适合但需工作任务"),
    (46, "t9-input", "T9 拼音输入法", "653-665", "触摸屏、TFTLCD、拼音/汉字词库", "UI、字库、存储检索", "输入法状态机运行在 UI 任务；词典查询可交给工作任务，候选结果带请求序号返回。", ["实现数字键到拼音组合的状态机和退格/翻页。", "验证词库索引、候选排序和中文显示。", "异步查询时丢弃过期结果，缓存常用索引页。"], "输入、修改、候选翻页和上屏行为正确；快速输入无旧结果覆盖新状态。", "词库占存储且随机访问频繁；编码必须与显示字库一致。", "适合但需异步查询"),
    (47, "uart-iap", "串口 IAP", "666-680", "USART、内部 Flash、Bootloader、应用分区", "向量表、链接脚本、升级协议、CRC", "推荐 Bootloader 保持小型裸机；RTOS 应用只负责接收/校验升级包并重启到 Bootloader 执行安全更新。", ["固定 Bootloader、应用、升级暂存和参数区布局。", "实现分包序号、长度、超时、CRC 和版本检查。", "断电测试写入与切换；跳转前关闭中断/外设并重定位 MSP 和向量表。"], "正常、丢包、重复包、坏包和断电后均能保留可启动镜像或进入恢复模式。", "这是高风险功能；错误擦写地址会变砖。量产应增加签名验证、防回滚和硬件恢复入口。", "可行，建议与 RTOS 解耦"),
    (48, "usb-cdc", "USB 虚拟串口", "681-690", "USB FS/PA11、PA12，PC 主机", "USB 描述符、时钟、端点缓冲", "USB 回调只搬运到流缓冲；USB 服务/协议任务完成解析和发送排队。", ["确认 USB 时钟和 CDC 描述符，验证主机枚举。", "做短包、满包和连续双向传输测试。", "设计端点忙时的排队/丢弃策略，避免在回调中阻塞。"], "多次插拔可重新枚举；持续传输无卡死，主机关闭端口后可恢复。", "PA11/PA12 与 CAN 冲突；USB 回调上下文不可调用阻塞 API。", "直接适合"),
    (49, "usb-msc", "USB 读卡器", "691-697", "USB FS、SD 卡、MSC 块接口", "USB、SD 块设备、介质所有权", "USB 存储服务独占介质；进入 MSC 模式前卸载 MCU 文件系统，退出后重新探测挂载。", ["先验证 SD 块读写，再实现 MSC 容量和读写回调。", "主机连接前 flush/unmount，切换明确的 OWNER_MCU/OWNER_USB 状态。", "测试安全弹出、强拔 USB、拔卡和写保护场景。"], "PC 可稳定识别、读写并安全弹出；切回 MCU 后文件系统一致。", "绝不能让 PC 和 MCU 同时写同一 FAT 卷；回调超时会导致主机重置设备。", "适合但需独占介质"),
    (50, "ucos-task-scheduling", "uC/OS-II 实验 1：任务调度", "698-711", "uC/OS-II、LED/按键/TFTLCD", "RTOS 端口、任务栈、SysTick", "该实验本身就是 RTOS 基础；在目标 RTOS 中建立两个以上任务验证优先级、延时和调度。", ["确认内核时基、中断优先级和任务栈初始化。", "建立独立 LED/UI 或统计任务，使用内核延时主动让出 CPU。", "观察优先级、时间片（若启用）和 CPU 使用率，并做栈水位检查。"], "任务按预期周期运行；高优先级任务不忙等；无栈溢出和系统节拍异常。", "不要照搬 uC/OS-II 的数值优先级语义到其他内核；HAL tick 与 RTOS tick 需统一。", "RTOS 原生实验"),
    (51, "ucos-semaphore-mailbox", "uC/OS-II 实验 2：信号量和邮箱", "712-723", "uC/OS-II、信号量、邮箱", "任务调度、ISR 安全 API", "用二值/计数信号量同步事件，用单槽邮箱传递指针或消息；明确对象和缓冲所有权。", ["构造生产者和消费者任务，先验证信号量唤醒。", "通过邮箱传递不可变消息或有明确归还规则的缓冲。", "加入超时、满/空和优先级反转测试。"], "事件不丢失、不重复；超时路径可恢复；缓冲不会被重复使用或泄漏。", "信号量不提供互斥量的优先级继承；邮箱传指针时尤其要防生命周期错误。", "RTOS 原生实验"),
    (52, "ucos-queue-event-timer", "uC/OS-II 实验 3：消息队列、信号量集和软件定时器", "724-743", "uC/OS-II、消息队列、事件/信号量集、软件定时器", "任务调度、内核对象、时间基准", "队列传递事件，事件组汇总状态，软件定时器触发轻量周期动作；回调不得阻塞。", ["用有界队列验证多条消息的顺序和满队列策略。", "用事件组等待任一/全部条件，并测试清位语义。", "软件定时器回调只发通知，把实际工作交给任务。"], "队列顺序、事件组合、定时周期均符合设计；压力下无死锁或无界积压。", "不同 RTOS 的事件清位、队列唤醒和定时器回调上下文不同，迁移时必须重新核对。", "RTOS 原生实验"),
]


def sop_filename(ch: int, slug: str) -> str:
    return f"SOP-{ch:02d}-{slug}.md"


def write_sop(item):
    ch, slug, title, pages, resources, prereq, design, steps, acceptance, risks, level = item
    step_lines = "\n".join(f"{idx}. {step}" for idx, step in enumerate(steps, 1))
    content = f"""# SOP-{ch:02d}：{title}

| 项目 | 内容 |
| --- | --- |
| 原始章节 | 第 {ch} 章 |
| 原文页码 | PDF 第 {pages} 页 |
| RTOS 适配判断 | {level} |
| 主要资源 | {resources} |

> 来源：{SOURCE}。本文是面向复现和 RTOS 迁移的操作清单，不替代芯片数据手册、开发板原理图和原书中的电气说明。

## 1. 实验目标

- 在裸机基线中复现“{title}”实验并保留可测量结果。
- 把底层驱动与业务逻辑分离，形成可被 RTOS 任务安全调用的接口。
- 对超时、资源冲突和失败恢复给出明确行为。

## 2. 前置条件与资源

- 前置能力：{prereq}。
- 硬件/外设：{resources}。
- 通用参考：[开发板硬件速查](../01_board_quick_reference.md)；接线、供电和复用关系以 V1.2 原书及实物原理图为准。
- 开始前保存当前时钟树、引脚复用、NVIC 和链接内存配置，避免覆盖已有实验资源。

## 3. RTOS 设计

{design}

通用约束：中断中只清标志、采集最小数据并使用 FromISR/ISR-safe API 通知任务；共享外设由单一服务任务或带优先级继承的互斥量保护；所有等待必须有超时和错误统计。

## 4. 操作步骤

{step_lines}
4. 把裸机示例中的长轮询或延时替换为状态机、任务阻塞等待或硬件触发。
5. 增加错误码、超时计数和关键状态日志；验证失败后可以重新初始化，而不是只能复位。
6. 在正常负载与压力负载下重复测试，并记录 CPU 占用、任务栈高水位和队列峰值。

## 5. 验收标准

- 功能：{acceptance}
- 实时性：关键中断不执行格式化输出、文件访问或长时间轮询；高优先级任务能按设计期限运行。
- 健壮性：断开外设、输入非法数据或制造超时后，驱动返回可诊断错误，系统其他任务继续运行。
- 可维护性：底层驱动、RTOS 适配层和应用逻辑分层，业务代码不直接依赖 HAL 全局句柄。

## 6. 风险与排障

- {risks}
- 若结果与原书不同，依次核查：板卡/模块版本、跳帽与供电、时钟树、GPIO 复用、NVIC 优先级、共享总线占用和 RTOS 栈空间。
- 建议保留一个最小裸机自检工程；遇到 RTOS 问题时先用它区分硬件/驱动问题与调度/同步问题。

## 7. 交付物

- 可复现的最小测试程序或测试命令。
- 一份资源声明：GPIO、DMA 通道、定时器、IRQ、任务优先级、栈和队列容量。
- 验收记录：测试条件、实测结果、失败注入结果和已知限制。
"""
    (OUT / sop_filename(ch, slug)).write_text(content, encoding="utf-8", newline="\n")


for experiment in EXPERIMENTS:
    write_sop(experiment)


index_rows = []
for ch, slug, title, pages, resources, prereq, design, steps, acceptance, risks, level in EXPERIMENTS:
    index_rows.append(f"| {ch} | [{title}]({sop_filename(ch, slug)}) | {pages} | {level} |")

index = f"""# 实验 SOP 索引

本目录把 {SOURCE} 第 6～52 章的 47 个实验逐一拆分为可执行 SOP。页码均指 PDF 页码。

使用顺序：先阅读上级目录中的 [实验到 RTOS 迁移 Map](../EXPERIMENT_RTOS_MAP.md)，再按依赖顺序执行单项 SOP。每份 SOP 都同时覆盖裸机复现、RTOS 改造和验收。

| 章 | 实验 SOP | PDF 页码 | RTOS 适配 |
| ---: | --- | ---: | --- |
{chr(10).join(index_rows)}
"""
(OUT / "README.md").write_text(index, encoding="utf-8", newline="\n")


def rtos_context(ch):
    if ch in {10, 13, 15, 26, 30, 32, 36, 38, 40, 48}:
        return "短 ISR + 工作任务"
    if ch in {11, 12}:
        return "监督任务"
    if ch in {14, 25}:
        return "硬件触发 + 控制任务"
    if ch in {17, 18, 31, 42, 43, 45, 46}:
        return "UI/显示任务"
    if ch in {37, 40, 41, 44, 47, 49}:
        return "存储/升级服务"
    if ch in {27, 28, 29, 33, 34, 35}:
        return "总线/传感服务"
    if ch >= 50:
        return "RTOS 原生任务"
    return "周期或事件任务"


map_rows = []
for ch, slug, title, pages, resources, prereq, design, steps, acceptance, risks, level in EXPERIMENTS:
    brief_resource = resources.replace("|", "/")
    map_rows.append(
        f"| {ch} | [{title}](sops/{sop_filename(ch, slug)}) | {brief_resource} | {rtos_context(ch)} | {level} |"
    )

map_doc = f"""# STM32F103 实验到 RTOS 迁移 Map

## 1. 结论

可行。{SOURCE} 的 47 个实验都能在 RTOS 上实现，其中 GPIO、UART、定时器、ADC/DMA、CAN 等可直接任务化；单总线时序、软件 I2C、电容触摸、看门狗、低功耗、文件系统、USB MSC、IAP 和摄像头需要改变原裸机组织方式，不能只把 `while(1)` 原样塞进多个任务。

推荐目标不是“每个实验一个永久任务”，而是把实验沉淀成驱动和服务，再由应用组合：

```text
应用/UI任务
   |  命令、事件、数据
   v
设备服务任务（串口、显示、传感器、存储、USB/网络）
   |  互斥总线、DMA缓冲、状态机
   v
HAL/BSP 驱动
   ^
   |  极短 ISR：清标志、时间戳、通知
硬件外设
```

## 2. 全部实验映射

页码和实验边界来自原书目录及正文。详细步骤见每行链接的 SOP。

| 章 | 实验 | 主要硬件/外设 | 建议执行上下文 | RTOS 适配 |
| ---: | --- | --- | --- | --- |
{chr(10).join(map_rows)}

## 3. 推荐实施波次

1. **基础 BSP**：统一时钟、GPIO、NVIC、日志、错误码、断言、微秒/毫秒时基和资源表。
2. **基础并发**：第 6～15 章，先完成 LED、蜂鸣器、按键、UART、EXTI、看门狗和定时器；建立任务、队列、互斥量和 ISR 通知规范。
3. **采集与波形**：第 16、22～26 章，形成 ADC/DMA、滤波和 PWM/DAC 的通用数据通道。
4. **总线与传感**：第 27～36 章，建立 I2C、SPI、UART/RS485、CAN 和传感器服务，解决共享总线与超时恢复。
5. **显示、时间与功耗**：第 17～21、31、42、45、46 章，统一 UI 事件流、显示所有权、RTC 和 suspend/resume。
6. **大数据与存储**：第 37～44、49 章，先做块设备和文件系统，再做图片、摄像头及 USB MSC；用有界缓冲和背压控制内存。
7. **升级与内核专题**：第 47、48、50～52 章，完成安全 IAP、USB CDC，并用后三个实验验证所选 RTOS 的核心原语。

## 4. RTOS 原语对照

SOP 使用的是抽象名称，可映射到不同内核：

| 意图 | FreeRTOS | RT-Thread | 使用原则 |
| --- | --- | --- | --- |
| 任务/线程 | `xTaskCreate` | `rt_thread_create` | 优先静态创建；记录栈高水位 |
| 消息队列 | `xQueue*` | `rt_mq_*` | 传值或传有明确所有权的缓冲 |
| 信号量 | `xSemaphore*` | `rt_sem_*` | 事件同步，不用于保护共享资源 |
| 互斥量 | Mutex API | `rt_mutex_*` | 保护总线/对象，利用优先级继承 |
| 事件组 | `xEventGroup*` | `rt_event_*` | 汇总多个健康位或系统状态 |
| 软件定时器 | `xTimer*` | `rt_timer_*` | 回调只通知，不做慢操作 |
| 任务延时 | `vTaskDelay/Until` | `rt_thread_mdelay` | 周期任务优先用绝对周期语义 |
| ISR 到任务 | FromISR API | `rt_*` 的中断安全调用 | ISR 只做最少工作，必要时触发调度 |

## 5. 必须提前定下的系统规则

- **中断优先级**：凡调用内核 API 的中断必须满足所选 RTOS 的优先级限制；NVIC 数值大小与逻辑优先级方向不要混淆。
- **时基**：明确 HAL tick 与 RTOS tick 的来源，避免两个 SysTick 处理互相覆盖；微秒时序使用独立硬件定时器。
- **单一所有权**：显示、UART TX、I2C、SPI、文件系统和 USB 存储应由服务任务独占，或至少以互斥量保护完整事务。
- **超时与恢复**：每次总线、队列、信号量和外设等待都设超时；记录超时、重试、溢出和复位原因。
- **内存**：F103 常见 SRAM 只有几十 KB；摄像头、图片、USB 和文件系统使用固定缓冲池、条带缓冲或零拷贝，禁止无界队列。
- **看门狗**：只有监督任务在所有关键任务都上报心跳后才能喂狗。
- **存储一致性**：Flash/FATFS 写入不在 ISR 中执行；进入待机、MSC 模式或升级前先同步并卸载。
- **时序型驱动**：DS18B20、DHT11、TPAD 和软件 I2C 使用硬件计时、输入捕获或很短的临界区，不允许长时间全局关中断。

## 6. 资源冲突清单

- PA0：KEY_UP、待机唤醒、TIM5_CH1 输入捕获。
- PA1：ADC1_CH1、TPAD/TIM5_CH2 相关实验。
- PA4：DAC 输出、MPU6050 中断信号。
- PA11/PA12：CAN 与 USB 共用，只能按构建或运行模式择一。
- PB5：DS0 与 TIM3_CH2 PWM 共用。
- PB13/PB14/PB15：SPI2 可能同时连接 Flash 和 NRF24L01，必须独立片选并仲裁总线。
- PG11：DS18B20 与 DHT11 单线实验共用。
- PA15：MPU6050 AD0 使用时需处理 JTAG 复用，建议保留 SWD 调试。

## 7. 推荐的项目目录

```text
bsp/                 # 时钟、GPIO、板级资源与引脚声明
drivers/             # 不依赖具体业务的设备驱动
services/            # uart、display、sensor、storage、watchdog 等单一所有者
app/                 # 状态机和业务任务
rtos_port/           # RTOS 原语适配与 ISR 通知封装
tests/               # 每个 SOP 对应的自检/失败注入用例
bootloader/          # 与主 RTOS 应用分离的 IAP
```

下一步建议先选择 FreeRTOS 或 RT-Thread，并以第 6、8、9、10、13 章作为第一条纵向切片：它们足以验证时基、任务、队列、ISR 通知、日志和资源分层是否正确。
"""
(ROOT / "docs" / "board_resources" / "EXPERIMENT_RTOS_MAP.md").write_text(map_doc, encoding="utf-8", newline="\n")

print(f"generated {len(EXPERIMENTS)} SOP files, index and map")
