# Ninja 高速构建工具

## 说明
用于存放 `ninja.exe` / `ninja` 可执行文件。

## 推荐下载
- **官方 Releases**：[Ninja Build Releases](https://github.com/ninja-build/ninja/releases)
- 解压后将单个 `ninja.exe` 放置在 `tools/ninja/` 目录下即可。
